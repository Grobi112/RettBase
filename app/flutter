#!/bin/bash
# Wrapper: Bei "build web" zuerst Version erhöhen, dann echtes Flutter ausführen.
# Nutzung: ./flutter build web  (aus dem app/-Verzeichnis)
if [ "$1" = "build" ] && [ "$2" = "web" ]; then
  cd "$(dirname "$0")"
  node web/increment_version.js
fi
exec command flutter "$@"
