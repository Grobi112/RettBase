import 'package:flutter/widgets.dart';

/// Stub: Iframe nur auf Web. Auf anderen Plattformen wird dies nicht verwendet.
Widget buildModuleIframe(String companyId, String modulePath) {
  return const SizedBox.shrink();
}
